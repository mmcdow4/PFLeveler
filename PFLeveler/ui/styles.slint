export global Styles {
    out property <length> window-init-height: 800px;
    out property <length> window-init-width: 1350px;
    out property <length> dialog-init-width: 200px;
    out property <length> dialog-init-height: 100px;
}

export component StyledText inherits Text {
    font-size: 14pt;
}